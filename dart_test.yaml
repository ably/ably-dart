paths: ["test/rest/unit", "test/realtime/unit"]

tags:
  integration:
  proxy:

presets:
  proxy:
    paths: ["test/rest/integration/proxy", "test/realtime/integration/proxy"]
  integration:
    paths: ["test/rest/integration", "test/realtime/integration"]
  all:
    paths: ["test"]
